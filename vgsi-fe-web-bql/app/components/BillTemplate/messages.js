/*
 * BillTemplate Messages
 *
 * This contains all the text for the BillTemplate component.
 */

import { defineMessages } from "react-intl";

export const scope = "app.components.BillTemplate";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BillTemplate component!"
  }
});
