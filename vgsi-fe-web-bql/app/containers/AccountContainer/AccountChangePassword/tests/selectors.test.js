// import { fromJS } from 'immutable';
// import { selectAccountChangePasswordDomain } from '../selectors';

describe('selectAccountChangePasswordDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
