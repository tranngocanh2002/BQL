/*
 * DashboardInvoiceBill Messages
 *
 * This contains all the text for the DashboardInvoiceBill container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashboardInvoiceBill";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashboardInvoiceBill container!"
  }
});
