/*
 * DashboardReception Messages
 *
 * This contains all the text for the DashboardReception container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashboardReception";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashboardReception container!"
  }
});
