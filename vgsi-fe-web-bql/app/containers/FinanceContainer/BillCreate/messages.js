/*
 * BillCreate Messages
 *
 * This contains all the text for the BillCreate container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BillCreate";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BillCreate container!"
  }
});
