/*
 * BillDetail Messages
 *
 * This contains all the text for the BillDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BillDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BillDetail container!"
  }
});
