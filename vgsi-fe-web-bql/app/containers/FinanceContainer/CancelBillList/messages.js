/*
 * CancelBillList Messages
 *
 * This contains all the text for the CancelBillList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CancelBillList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CancelBillList container!"
  }
});
