/*
 * CashBook Messages
 *
 * This contains all the text for the CashBook container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.CashBook";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the CashBook container!"
  }
});
