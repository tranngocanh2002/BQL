/*
 * DashboardDebt Messages
 *
 * This contains all the text for the DashboardDebt container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.DashboardDebt";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the DashboardDebt container!"
  }
});
