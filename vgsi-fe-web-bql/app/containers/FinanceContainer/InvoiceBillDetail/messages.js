/*
 * InvoiceBillDetail Messages
 *
 * This contains all the text for the InvoiceBillDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InvoiceBillDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InvoiceBillDetail container!"
  }
});
