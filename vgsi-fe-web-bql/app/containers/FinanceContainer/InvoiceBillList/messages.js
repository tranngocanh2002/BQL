/*
 * InvoiceBillList Messages
 *
 * This contains all the text for the InvoiceBillList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InvoiceBillList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InvoiceBillList container!"
  }
});
