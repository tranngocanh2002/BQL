// import { fromJS } from 'immutable';
// import { selectNotificationFeeListDomain } from '../selectors';

describe('selectNotificationFeeListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
