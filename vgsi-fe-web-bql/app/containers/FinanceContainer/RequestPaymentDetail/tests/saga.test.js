/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */

describe("requestPaymentDetailSaga Saga", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
