// import { fromJS } from 'immutable';
// import { selectHistoryAccessControlDomain } from '../selectors';

describe('selectHistoryAccessControlDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
