/*
 * HistoryCarpacking Messages
 *
 * This contains all the text for the HistoryCarpacking container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.HistoryCarpacking";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the HistoryCarpacking container!"
  }
});
