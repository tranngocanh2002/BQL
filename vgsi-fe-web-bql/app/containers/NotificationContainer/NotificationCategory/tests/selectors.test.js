// import { fromJS } from 'immutable';
// import { selectNotificationCategoryDomain } from '../selectors';

describe('selectNotificationCategoryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
