/*
 * NotificationDetail Messages
 *
 * This contains all the text for the NotificationDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.NotificationDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NotificationDetail container!"
  }
});
