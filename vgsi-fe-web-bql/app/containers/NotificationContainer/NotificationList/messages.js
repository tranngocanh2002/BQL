/*
 * NotificationList Messages
 *
 * This contains all the text for the NotificationList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.NotificationList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NotificationList container!"
  }
});
