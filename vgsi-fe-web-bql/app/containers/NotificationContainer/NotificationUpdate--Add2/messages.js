/*
 * NotificationUpdate Messages
 *
 * This contains all the text for the NotificationUpdate container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.NotificationUpdate";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the NotificationUpdate container!"
  }
});
