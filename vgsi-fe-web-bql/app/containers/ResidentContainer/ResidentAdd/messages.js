/*
 * ResidentAdd Messages
 *
 * This contains all the text for the ResidentAdd container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentAdd";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentAdd container!"
  }
});
