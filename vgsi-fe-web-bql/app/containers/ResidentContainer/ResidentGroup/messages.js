/*
 * ResidentGroup Messages
 *
 * This contains all the text for the ResidentGroup container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentGroup";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentGroup container!"
  }
});
