/*
 * ResidentList Messages
 *
 * This contains all the text for the ResidentList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentList container!"
  }
});
