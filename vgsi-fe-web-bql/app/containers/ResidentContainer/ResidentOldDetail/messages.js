/*
 * ResidentOldDetail Messages
 *
 * This contains all the text for the ResidentOldDetail container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentOldDetail";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentOldDetail container!"
  }
});
