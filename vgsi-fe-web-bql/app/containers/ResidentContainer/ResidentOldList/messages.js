/*
 * ResidentOldList Messages
 *
 * This contains all the text for the ResidentOldList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentOldList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentOldList container!"
  }
});
