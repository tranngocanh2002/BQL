/*
 * LockFeeTemplatePage Messages
 *
 * This contains all the text for the LockFeeTemplatePage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.LockFeeTemplatePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the LockFeeTemplatePage container!"
  }
});
