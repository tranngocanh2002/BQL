/*
 * PaymentManagementClusterPage Messages
 *
 * This contains all the text for the PaymentManagementClusterPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PaymentManagementClusterPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PaymentManagementClusterPage container!"
  }
});
