// import { fromJS } from 'immutable';
// import { selectPaymentManagementClusterPageDomain } from '../selectors';

describe('selectPaymentManagementClusterPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
