/*
 * PaymentTemplateManagementClusterPage Messages
 *
 * This contains all the text for the PaymentTemplateManagementClusterPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PaymentTemplateManagementClusterPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PaymentTemplateManagementClusterPage container!"
  }
});
