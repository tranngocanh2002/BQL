// import { fromJS } from 'immutable';
// import { selectPaymentTemplateManagementClusterPageDomain } from '../selectors';

describe('selectPaymentTemplateManagementClusterPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
