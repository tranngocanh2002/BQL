/*
 * PaymentMotoPackingPage Messages
 *
 * This contains all the text for the PaymentMotoPackingPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.PaymentMotoPackingPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the PaymentMotoPackingPage container!"
  }
});
