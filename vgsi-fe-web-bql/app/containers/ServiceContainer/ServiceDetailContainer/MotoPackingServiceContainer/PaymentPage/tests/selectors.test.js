// import { fromJS } from 'immutable';
// import { selectPaymentMotoPackingPageDomain } from '../selectors';

describe('selectPaymentMotoPackingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
