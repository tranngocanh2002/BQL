/*
 * VihicleManagement Messages
 *
 * This contains all the text for the VihicleManagement container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.VihicleManagement";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the VihicleManagement container!"
  }
});
