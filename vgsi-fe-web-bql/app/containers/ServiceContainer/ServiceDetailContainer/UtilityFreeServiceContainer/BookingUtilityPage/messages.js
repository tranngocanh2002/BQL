/*
 * BookingUtilityPage Messages
 *
 * This contains all the text for the BookingUtilityPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BookingUtilityPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BookingUtilityPage container!"
  }
});
