/*
 * ListUltilityPage Messages
 *
 * This contains all the text for the ListUltilityPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ListUltilityPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ListUltilityPage container!"
  }
});
