// import { fromJS } from 'immutable';
// import { selectWaterServiceContainerDomain } from '../selectors';

describe('selectWaterServiceContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
