/*
 * ServiceProviderAdd Messages
 *
 * This contains all the text for the ServiceProviderAdd container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ServiceProviderAdd";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ServiceProviderAdd container!"
  }
});
