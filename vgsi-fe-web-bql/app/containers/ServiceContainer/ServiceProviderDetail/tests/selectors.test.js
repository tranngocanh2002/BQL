// import { fromJS } from 'immutable';
// import { selectServiceProviderDetailDomain } from '../selectors';

describe('selectServiceProviderDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
