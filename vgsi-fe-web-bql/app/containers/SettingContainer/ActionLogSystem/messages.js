/*
 * ActionLogSystem Messages
 *
 * This contains all the text for the ActionLogSystem container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ActionLogSystem";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ActionLogSystem container!"
  }
});
