/*
 * BuildingClusterInfomation Messages
 *
 * This contains all the text for the BuildingClusterInfomation container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.BuildingClusterInfomation";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the BuildingClusterInfomation container!"
  }
});
