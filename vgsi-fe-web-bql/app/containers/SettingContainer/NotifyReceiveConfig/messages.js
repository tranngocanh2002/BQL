/*
 *  Notify Receive Config Messages
 *
 * This contains all the text for the Notify Receive Config container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NotifyReceiveConfig';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Notify Receive Config container!',
  },
});
