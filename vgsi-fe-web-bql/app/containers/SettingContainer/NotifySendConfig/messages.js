/*
 *  Notify Send Config Messages
 *
 * This contains all the text for the Notify Send Config container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NotifySendConfig';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Notify Send Config container!',
  },
});
