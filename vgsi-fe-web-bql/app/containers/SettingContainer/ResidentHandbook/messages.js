/*
 * ResidentHandbook Messages
 *
 * This contains all the text for the ResidentHandbook container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.ResidentHandbook";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the ResidentHandbook container!"
  }
});
