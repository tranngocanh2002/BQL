/*
 * InfomationElectricPage Messages
 *
 * This contains all the text for the InfomationElectricPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InfomationElectricPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InfomationElectricPage container!"
  }
});
