// import { fromJS } from 'immutable';
// import { selectInfomationElectricPageDomain } from '../selectors';

describe('selectInfomationElectricPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
