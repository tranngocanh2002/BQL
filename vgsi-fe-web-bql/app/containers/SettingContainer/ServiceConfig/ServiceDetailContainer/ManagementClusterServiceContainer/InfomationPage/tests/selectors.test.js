// import { fromJS } from 'immutable';
// import { selectInfomationManagementClusterPageDomain } from '../selectors';

describe('selectInfomationManagementClusterPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
