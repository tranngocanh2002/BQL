// import { fromJS } from 'immutable';
// import { selectInfomationMotoPackingPageDomain } from '../selectors';

describe('selectInfomationMotoPackingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
