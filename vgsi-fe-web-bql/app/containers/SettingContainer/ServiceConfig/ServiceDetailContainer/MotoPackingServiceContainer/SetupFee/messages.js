/*
 * SetupFeeMotoPackingPage Messages
 *
 * This contains all the text for the SetupFeeMotoPackingPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.SetupFeeMotoPackingPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SetupFeeMotoPackingPage container!"
  }
});
