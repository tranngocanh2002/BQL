// import { fromJS } from 'immutable';
// import { selectSetupFeeMotoPackingPageDomain } from '../selectors';

describe('selectSetupFeeMotoPackingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
