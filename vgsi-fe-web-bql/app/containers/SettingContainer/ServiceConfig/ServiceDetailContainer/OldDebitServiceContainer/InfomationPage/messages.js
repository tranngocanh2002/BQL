/*
 * InfomationOldDebitPage Messages
 *
 * This contains all the text for the InfomationOldDebitPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InfomationOldDebitPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InfomationOldDebitPage container!"
  }
});
