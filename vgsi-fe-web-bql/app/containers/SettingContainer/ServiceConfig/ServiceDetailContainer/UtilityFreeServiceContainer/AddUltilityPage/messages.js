/*
 * AddUltilityPage Messages
 *
 * This contains all the text for the AddUltilityPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.AddUltilityPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the AddUltilityPage container!"
  }
});
