/*
 * InfomationUtiliityFreePage Messages
 *
 * This contains all the text for the InfomationUtiliityFreePage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InfomationUtiliityFreePage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InfomationUtiliityFreePage container!"
  }
});
