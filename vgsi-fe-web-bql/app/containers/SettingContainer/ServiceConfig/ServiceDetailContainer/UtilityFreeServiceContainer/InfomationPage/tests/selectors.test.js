// import { fromJS } from 'immutable';
// import { selectInfomationUtiliityFreePageDomain } from '../selectors';

describe('selectInfomationUtiliityFreePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
