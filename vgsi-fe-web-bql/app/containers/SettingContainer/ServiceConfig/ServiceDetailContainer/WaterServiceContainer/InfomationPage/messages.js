/*
 * InfomationWaterPage Messages
 *
 * This contains all the text for the InfomationWaterPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.InfomationWaterPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the InfomationWaterPage container!"
  }
});
