// import { fromJS } from 'immutable';
// import { selectInfomationWaterPageDomain } from '../selectors';

describe('selectInfomationWaterPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
