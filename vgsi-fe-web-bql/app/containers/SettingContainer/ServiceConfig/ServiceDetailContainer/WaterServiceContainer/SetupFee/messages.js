/*
 * SetupFeeWaterPage Messages
 *
 * This contains all the text for the SetupFeeWaterPage container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.SetupFeeWaterPage";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SetupFeeWaterPage container!"
  }
});
