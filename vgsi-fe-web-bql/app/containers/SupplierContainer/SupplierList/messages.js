/*
 * SupplierList Messages
 *
 * This contains all the text for the SupplierList container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.SupplierList";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the SupplierList container!",
  },
});
